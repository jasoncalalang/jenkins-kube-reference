apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: registry-data
  namespace: build
spec:
  accessModes: [ReadWriteOnce]
  resources:
    requests:
      storage: 2Gi
  storageClassName: standard
